# Compiled binaries — build from source
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go build & test output
*.out
*.test
/vendor/
go.work
go.work.sum

# Local runtime, credentials and scratch — never publish (match in any folder)
config.json
calendarr.db
*.log

# Personal-only installer (not part of the public release)
/internal/installer/

# Local scratch notes / reference assets
/saison.jpg
/.[Ii]dée.txt

# Cross-platform release binaries (uploaded to Releases, not committed)
/dist/
